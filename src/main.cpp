#include <iostream>

int main(int argc, char* argv[])
{
    if (argc < 2)
    {
        std::cout << "Usage: " << argv[0] << " <command>" << std::endl;
        return 0;
    }

    // std::string command = argv[1];

    return 0;
}